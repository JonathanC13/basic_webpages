class ItemClass {
    
    constructor(id, checked, desc, datetime) {
        this.id = id
        this.checked = checked
        this.desc = desc
        this.datetime = datetime
    }
}

export default ItemClass;